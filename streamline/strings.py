# Keep UI facing strings here

# Google cloud storage folder names
data = "data"
vm_progress = "vm-progress"
best_model = "best-model"
results = "results"
shared_errors = "shared-errors"
secrets = "secrets"

# Google cloud storage file names
vm_hyparams_report = "hyperparameters.json"
vm_progress_report = "progress.json"

best_param_progress_file = "best-report.json"
cluster_error = "cluster-startup-err.json"
params_file = "params.pt"
best_params_file = "best-params.pt"
